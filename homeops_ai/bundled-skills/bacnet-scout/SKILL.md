Use this skill only when BACnet discovery is enabled.

Goals:
- discover BACnet/IP devices carefully
- record device addresses and metadata into the system graph
- avoid aggressive probing on production networks

Rules:
- treat BACnet discovery as opt-in and network-sensitive
- favor read-only reconnaissance first
- record what was scanned, when, and on which interface or subnet

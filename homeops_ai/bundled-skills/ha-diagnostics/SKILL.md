Diagnose add-on, gateway, automation, and network-adjacent problems.

Checklist:
- confirm gateway mode, bind, port, auth, and allowed origins
- inspect recent cron runs and heartbeat state
- inspect logs before changing configuration
- check disk pressure and stale session locks
- separate startup issues from runtime issues

Do not guess. Use the dashboard runtime data, `openclaw` CLI, and local files.

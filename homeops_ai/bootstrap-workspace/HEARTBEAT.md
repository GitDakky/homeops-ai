Recurring operator reminders:

- Check for OpenClaw, Home Assistant, and integration release drift before recommending changes.
- Review recent cron runs and the last heartbeat event when diagnosing strange automation behavior.
- Watch disk pressure and stale lock files before blaming the application layer.
- Keep the system graph current enough to answer: what exists, what talks to what, where it lives, and what IP it uses.
- If credentials are missing, say so directly and point to the correct secrets path instead of guessing.

name: Validate add-on

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run baseline local validation
        run: bash scripts/validate_local.sh
        env:
          VALIDATION_BASE_REF: ${{ github.event.pull_request.base.sha || github.event.before }}

  docker-smoke-amd64:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v4

      - name: Build add-on image (amd64 smoke test)
        run: |
          docker build \
            --platform linux/amd64 \
            --build-arg BUILD_ARCH=amd64 \
            -f homeops_ai/Dockerfile \
            -t openclaw-assistant-ci:amd64 \
            homeops_ai

      - name: Smoke test bundled tooling
        run: |
          docker run --rm --entrypoint /bin/bash openclaw-assistant-ci:amd64 -lc '
            test -x /run.sh
            test -f /oc_config_helper.py
            test -f /render_nginx.py
            openclaw --version
            node -v
            python3 --version
          '
